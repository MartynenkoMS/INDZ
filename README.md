# Метеосервіс - MVC Архітектура

## Про проект

Повнофункціональний прототип системи метеорологічного сервісу, розроблений з використанням **паттерну MVC (Модель-Вигляд-Контролер)** та принципів об'єктно-орієнтованого програмування. Система дозволяє керувати метеорологічними даними, керувати підписниками, розповсюджувати попередження та сповіщення через REST API та веб-інтерфейс.

### 🎯 Основні можливості:

- ✅ **MVC архітектура** - Чіткий поділ на Модель, Вигляд та Контролер
- ✅ **REST API** - JSON API для отримання даних (`/api/subscribers`, `/api/status`)
- ✅ **Веб-інтерфейс** - Інтерактивна сторінка з Live Server та навігацією
- ✅ **Управління підписниками** - 12+ підписників у SQLite БД
- ✅ **Прогноз погоди** - Прогноз на 5 днів з датами та деталями
- ✅ **Система сповіщень** - Відправка попереджень підписникам
- ✅ **Dependency Injection** - Google Guice для управління залежностями
- ✅ **CORS підтримка** - Для роботи з Live Server та GitHub Codespaces

## Архітектура MVC

### 📊 Model (Модель)
- **ServiceNotifier** - Управління підписниками та сповіщеннями (композиція)
- **SubscriberService** - Взаємодія з БД SQLite
- **Subscriber** - Об'єкт підписника

### 🖼️ View (Вигляд)
- **WeatherWebView** - REST API для веб-інтерфейсу
- **index.html** - Веб-сторінка з інтерактивними розділами
- **script.js** - JavaScript логіка для завантаження даних
- **style.css** - Стилізація веб-сторінки

### 🎮 Controller (Контролер)
- **WeatherController** - Проміжна ланка між View та Model
- Обробка запитів від View
- Взаємодія з Model для отримання/збереження даних

### 🌐 Web Server
- **WebServer** (абстрактний) - Інтерфейс для веб-сервера
- **JavalinWebServer** - Реалізація на базі Javalin 5.6.3
- Работа на порту 8080

## Структура проєкту

```
weatherservice/
├── src/
│   ├── main/java/com/weatherservice/
│   │   ├── Main.java                      - Точка входу програми
│   │   ├── WeatherController.java         - MVC Контролер
│   │   ├── WeatherWebView.java            - MVC Вигляд (REST API)
│   │   ├── ServiceNotifier.java           - Модель для управління підписниками
│   │   ├── SubscriberService.java         - Сервіс БД для підписників
│   │   ├── Subscriber.java                - Клас підписника
│   │   ├── weatherserviceModule.java      - Guice модуль для інжекції залежностей
│   │   ├── Meteorologist.java             - Метеоролог (наслідується від WeatherSystemParticipant)
│   │   ├── Technician.java                - Техніч (наслідується від WeatherSystemParticipant)
│   │   ├── WeatherSystemParticipant.java  - Абстрактний клас учасника системи
│   │   ├── INotificationPlan.java         - Інтерфейс для сповіщень (реалізація)
│   │   ├── webserver/
│   │   │   ├── WebServer.java             - Абстрактний клас веб-сервера
│   │   │   ├── JavalinWebServer.java      - Реалізація на базі Javalin
│   │   │   ├── HttpContext.java           - Інтерфейс HTTP контексту
│   │   │   ├── JavalinHttpContext.java    - Реалізація контексту для Javalin
│   │   │   ├── Request.java               - Інтерфейс HTTP запиту
│   │   │   └── Response.java              - Інтерфейс HTTP відповіді
│   │   └── resources/
│   │       ├── index.html                 - Веб-сторінка з розділами
│   │       ├── script.js                  - JavaScript для API запитів
│   │       └── style.css                  - Стилі веб-сторінки
│   └── test/java/com/weatherservice/
│       └── AppTest.java                   - Тести проекту
├── docs/
│   ├── mvc.class.puml                     - UML діаграма MVC архітектури
│   ├── class.puml                         - Діаграма всіх класів
│   ├── usecase.puml                       - Діаграма прецедентів
│   ├── sequence.mmd                       - Діаграма послідовності
│   ├── state.mmd                          - Діаграма станів
│   └── requirements.md                    - Вимоги проекту
├── pom.xml                                - Конфігурація Maven
└── README.md                              - Цей файл
```

## 4 Типи відносин OOP

Проект демонструє всі чотири типи відносин між класами:

1. **Наслідування (Inheritance)**
   - `Meteorologist` та `Technician` наслідуються від `WeatherSystemParticipant`
   - `JavalinWebServer` наслідується від `WebServer`

2. **Реалізація (Implementation)**
   - `Meteorologist` реалізує інтерфейс `INotificationPlan`
   - `JavalinHttpContext` реалізує `HttpContext`

3. **Залежність (Dependency)**
   - `SubscriberService` залежить від `Connection` (SQL)
   - `WeatherWebView` залежить від `WeatherController` та `WebServer`

4. **Композиція (Composition)**
   - `ServiceNotifier` містить список `Subscriber` об'єктів
   - `WeatherWebView` містить `WeatherController` та `WebServer`

## 📦 Залежності

```xml
<!-- Google Guice для інжекції залежностей -->
<dependency>
    <groupId>com.google.inject</groupId>
    <artifactId>guice</artifactId>
    <version>5.1.0</version>
</dependency>

<!-- SQLite JDBC для роботи з БД -->
<dependency>
    <groupId>org.xerial</groupId>
    <artifactId>sqlite-jdbc</artifactId>
    <version>3.36.0.3</version>
</dependency>

<!-- Javalin для REST API -->
<dependency>
    <groupId>io.javalin</groupId>
    <artifactId>javalin</artifactId>
    <version>5.6.3</version>
</dependency>

<!-- SLF4J для логування -->
<dependency>
    <groupId>org.slf4j</groupId>
    <artifactId>slf4j-api</artifactId>
    <version>2.0.9</version>
</dependency>

<!-- Jackson для JSON обробки -->
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>2.15.0</version>
</dependency>
```

## ⚙️ Системні вимоги

- **Java**: 11 LTS або вище
- **Maven**: 3.6 або вище
- **Browser**: Будь-який сучасний браузер для веб-інтерфейсу

## 🛠️ Компіляція

Для компіляції проекту виконайте команду з директорії `weatherservice/`:

```bash
mvn clean compile
```

Або для повної збірки з тестами:

```bash
mvn clean install
```

## 🚀 Запуск

### За допомогою Maven:

```bash
cd weatherservice
mvn exec:java -Dexec.mainClass="com.weatherservice.Main"
```

### Результат:
- Програма додасть 12 тестових підписників до БД
- Демонструватиме роботу метеоролога та сповіщень
- Запустить веб-сервер на `http://localhost:8080`

### Доступ до веб-інтерфейсу:

1. **Локально**: Відкрийте файл `weatherservice/src/main/java/com/weatherservice/resources/index.html` з Live Server
2. **API endpoints**:
   - `GET /api/status` - Статус системи (JSON)
   - `GET /api/subscribers` - Список всіх підписників (JSON)

## 📊 REST API

### GET /api/status
Повертає інформацію про статус системи:

```json
{
  "service": "Weather Service",
  "version": "1.0",
  "status": "running",
  "subscribers_count": 12
}
```

### GET /api/subscribers
Повертає список всіх підписників:

```json
[
  {"name": "John Smith"},
  {"name": "Alice Johnson"},
  {"name": "Bob Wilson"},
  {"name": "Emma Davis"},
  {"name": "Michael Brown"},
  {"name": "Sarah Miller"},
  {"name": "James Wilson"},
  {"name": "Rachel Taylor"},
  {"name": "David Anderson"},
  {"name": "Jessica Thomas"},
  {"name": "Christopher Lee"},
  {"name": "Victoria Martinez"}
]
```

## 🧪 Тестування

Запустіть тести проекту:

```bash
mvn test
```

## 📚 Документація

- **`docs/mvc.class.puml`** - UML діаграма MVC архітектури (PlantUML)
- **`docs/class.puml`** - Повна діаграма всіх класів (PlantUML)
- **`docs/usecase.puml`** - Діаграма прецедентів (PlantUML)
- **`docs/sequence.mmd`** - Діаграма послідовності (Mermaid)
- **`docs/state.mmd`** - Діаграма станів (Mermaid)
- **`docs/requirements.md`** - Детальні вимоги проекту

## 💾 База даних

Проект використовує **SQLite** з автоматичним створенням таблиці при першому запуску:

```sql
CREATE TABLE IF NOT EXISTS subscribers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
)
```

Файл БД розташовується за адресою: `weatherservice/target/weatherservice.db`

## 🔄 Інжекція залежностей (Dependency Injection)

Проект використовує **Google Guice** для управління залежностями. Конфігурація знаходиться в `weatherserviceModule.java`:

- **Singleton** `Connection` - одне з'єднання з БД для всього додатку
- **Setter injection** - залежності вводяться через методи `@Inject`
- **Constructor injection** - залежності вводяться через конструктор де необхідно

## 👨‍💼 Автори

Метеосервіс розроблений як навчальний проект для демонстрації:
- Принципів об'єктно-орієнтованого програмування (OOP)
- Паттернів проектування (MVC, Singleton, Dependency Injection)
- REST API та веб-послуг
- Роботи з базами даних SQL

## 📝 Ліцензія

Цей проект створений у навчальних цілях.
